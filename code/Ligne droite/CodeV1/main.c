#define FCY 6000000     // ajout des constantes
#include <xc.h>         // Contains register variable definitions
#include "../../libs/Encoders.h" // Ajout des libraires


int main(void) {
    // setup des variables et des intéruptions
    while(1) {
        // Code a tourner en boucle
    } 
    return 0;
}
